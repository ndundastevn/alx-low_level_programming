#include <stdio.h>
#include "main.h"
 
/**
 * main - Entry point for the program
 * Description: perform a series of actions
 * Return: 0
*/
int main(void)
{

  char* str;
  int len;

 len =  _strlen(str);
  
return (0);
}
